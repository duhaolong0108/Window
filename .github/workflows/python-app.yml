name: Greeting from Mona
on: push
jobs:
  job1:
    name: A
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"
      
      - id: R
        run: |
          python Main.py
